/*public class Time {
    private String timeslot; // The time slot, e.g., "9:00 AM"
    private boolean available; // Indicates whether the time slot is available

    public Time(String timeslot, boolean available) {
        this.timeslot = timeslot;
        this.available = available;
    }

    public String getTimeslot() {
        return timeslot;
    }

    public void setTimeslot(String time) {
        this.timeslot = time;
    }

    public boolean isAvailable() {
        return available;
    }

    public void setAvailable(boolean available) {
        available = available;
    }

    @Override
    public String toString() {
        if (available) {
            return timeslot + " (Available)";
        }
        else {
            return timeslot + " (Not Available)";
        }
    }
}*/
